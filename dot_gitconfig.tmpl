[user]
  name = {{ .data.git.name }}
  email = {{ .data.git.email }}

[alias]
  st = status
  co = checkout
  ci = commit
  br = branch
  lg = log --oneline --decorate --graph --all

[core]
  editor = {{ .data.git.editor | default "nvim" }}
